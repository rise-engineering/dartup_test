name: dartup_test
description: upy project lib.
version: 1.0.0+2
homepage: https://gitlab.com/join-up/mobile/main/dartup_test.git
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  flutter_bloc: ^7.0.1
  # Value equality
  equatable: ^2.0.3
  # Functional programming thingies
  dartz: ^0.10.0-nullsafety.2
  # Service locator
  get_it: ^7.1.3
  faker: ^2.0.0
  test: ^1.16.8
  bloc_test: ^8.0.2
  mocktail: ^0.1.4
  easy_localization: ^3.0.0
  flutter_test:
    sdk: flutter


# dev_dependencies:
#   integration_test:
#     sdk: flutter
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
# for platform specific see
# https://github.com/mogol/flutter_secure_storage/blob/develop/pubspec.yaml
#flutter:
#  plugin:
#    androidPackage: io.join.UpyPlugin
#    pluginClass: UpyPlugin
